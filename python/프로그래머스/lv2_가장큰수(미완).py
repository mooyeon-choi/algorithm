def solution(numbers):
    answer = ''
    left, right = numbers[0], numbers[-1]
    def quick(left, right):
        mid = left
        left += 1
        while left < right:
    
    def compare(std, comp):
        if len(std) == len(comp):
            if std > comp:
                return False
            else:
                return True
        elif len(std) < len(comp):
            for i in range(0, len(comp))
        
    return answer